(function($) {
	$(function() {
		$(".unslider").unslider({
			keys: true,
			dots: false,               
			fluid: false 
		});



		// var $widget_rsg = $('.widget_rsg');

		// if ($widget_rsg.length) {
		// 	$rsgallery = $('.rsgallery');
		// 	$rsg_items = $rsgallery.find('.rsg_item');
		// 	var width = $rsg_items.length * 160;
		// 	$rsgallery.css("width", width + "px");
		// 	var containerWidth = $widget_rsg.width();
		// 	console.log("containerWidth : " + containerWidth);

		// 	if (width > containerWidth) {
		// 		rsg_move($rsgallery, width, containerWidth);
		// 	}
		// }

		// function rsg_move($rsgallery, width, containerWidth) {
		// 	if (width > containerWidth) {
		// 		var speed = 10000;
		// 		var easing = null;
		// 		$rsgallery.css("left", 0);
		// 		$rsgallery.animate({
		// 			"left": (containerWidth - width) + "px"
		// 		}, speed, easing, function() {
		// 			rsg_move($rsgallery, width, containerWidth);
		// 		});
		// 	}
		// }

	});



})(jQuery);